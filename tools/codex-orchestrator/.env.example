# No API key is used by this project.
# Authentication is managed by the local Codex App Server and ChatGPT device-code login.
CODEX_BIN=codex
# Optional explicit model mode. The CLI also accepts --model and --effort directly.
# CODEX_ORCHESTRATOR_MODEL=gpt-5.5
# CODEX_ORCHESTRATOR_EFFORT=high
